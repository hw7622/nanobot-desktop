{
  "name": "nanobot-desktop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:backend": "python run_backend.py",
    "dev": "cargo tauri dev",
    "build": "cargo tauri build"
  }
}
